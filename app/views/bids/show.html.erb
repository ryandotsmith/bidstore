<div id="center">
	<h1>back to <%= link_to @bid.customer.name, @bid.customer %></h1>
	<h2><%="PowerPro ID: " + @bid.customer.powerpro_id %></h2>
	<%= render :partial => 'lanes/list', :locals => { :lanes => @bid.lanes }%>
	<%= link_to "add lanes", edit_bid_path(@bid) %>
</div><!--end center-->